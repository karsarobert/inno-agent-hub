# Observation log – Python Basics

The purpose of this log is not just to run the programs, but also to record
what you **predicted**, what you saw, and how you explain the difference.

## Lesson 1 – code and behaviour

### `lecke1_1_hello.py`
- **Before running, I expected:**
- **The actual output, in short:**
- **Why does the `5` appear next to the quoted texts?**
- **What do I think `PRINT(...)` would do?**

### `lecke1_2_tipusok.py`
- **The four types and my own example:**
  - `bool`:
  - `int`:
  - `float`:
  - `str`:
- **What is the difference between `True` and `"True"`?**
- **What did `type("42")` report? Why?**

### `lecke1_3_operatorok.py`
- **Before running, I expected this for `5 / 2`:**
- **Before running, I expected this for `5 // 2`:**
- **What is the difference between `/` and `//`?**
- **What did `"ha" * 5` produce? How do I explain it?**
- **Why do we use `+= 1` instead of `++`?**

## Lessons 2–4 – experimentation

- **Which modification did I make in `lecke2_formazas.py`?**
- **What type did the `val` variable have after each assignment?**
- **What result did I get with the two numbers in lesson 4?**
- **What would cause an error if I added the `input()` results without
  conversion?**

## Short self-assessment

Things I can already do:

- [ ] create a Python file at the root of the workspace
- [ ] run a Python file in the terminal below the workspace
- [ ] print values with `print()`
- [ ] distinguish the `int`, `float`, `str`, `bool` types
- [ ] use f-strings
- [ ] read data with `input()` and convert it to a number with `int()`

My next question to the Inno Agent:

>
