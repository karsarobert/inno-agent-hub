#include <iostream>

int main() {
    const int first_number = 1;
    const int last_number = 10;
    int even_count{};

    // TODO:
    // 1. Traverse the range with a for loop.
    // 2. Use if to check whether the current number is even.
    // 3. Increase even_count when the number is even.

    // TODO: Print: Number of even numbers: <value>

    return 0;
}
