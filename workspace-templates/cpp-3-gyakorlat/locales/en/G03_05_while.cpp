#include <iostream>

int main() {
    const int last_number = 5;
    int current_number = 1;

    // TODO: Write a while loop that prints the numbers from 1 to 5.
    // Print each number on a separate line.

    return 0;
}
