#include <iostream>

int main() {
    const int first_number = 1;
    const int last_number = 12;
    int divisible_count{};

    // OPTIONAL EXTRA PRACTICE:
    // Traverse the range first_number..last_number.
    // Print the numbers divisible by 3 and count them.

    // TODO: At the end print: Count: <value>

    return 0;
}
